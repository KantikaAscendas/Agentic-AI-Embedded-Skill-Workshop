# Agentic AI Workshop Files

Run `scripts/workshopPreflight.m` before the event. Verified Part 1 and Part 2 scripts are retained under `reference` as instructor fallbacks and examples of the evidence-producing workflow.

Agents should place new visible scripts in `generated`, outputs in `results`, and code-generation artifacts in `build`. These generated folders are not committed unless an instructor reference artifact is intentionally approved.
